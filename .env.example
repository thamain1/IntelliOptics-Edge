# IntelliOptics Edge – sample environment (no secrets)
INTELLIOPTICS_ENDPOINT=https://intellioptics-api-37558.azurewebsites.net
INTELLIOPTICS_API_TOKEN=REPLACE_ME

# Queues / messaging
SERVICE_BUS_CONN=REPLACE_ME
SB_IMAGE_QUEUE=image-queries
SB_RESULTS_QUEUE=inference-results
SB_FEEDBACK_QUEUE=feedback

# Storage for model/artifacts
AZURE_STORAGE_CONNECTION_STRING=REPLACE_ME
AZ_BLOB_CONTAINER=images
MODEL_URI=REPLACE_ME
MODEL_CACHE_DIR=/tmp/model

# Container registry (build/push)
REGISTRY_PROVIDER=azure

# Azure Container Registry defaults
ACR_LOGIN_SERVER=acrintellioptics.azurecr.io
ACR_NAME=acrintellioptics
ACR_RESOURCE_GROUP=intellioptics-edge
ACR_USERNAME=REPLACE_ME
ACR_PASSWORD=REPLACE_ME
