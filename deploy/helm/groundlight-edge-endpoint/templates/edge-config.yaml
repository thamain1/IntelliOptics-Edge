apiVersion: v1
kind: ConfigMap
metadata:
  name: edge-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "IntelliOptics-edge-endpoint.labels" . | nindent 4 }}
data:
  edge-config.yaml: |
    {{- include "IntelliOptics-edge-endpoint.edgeConfig" . | nindent 4 }}

