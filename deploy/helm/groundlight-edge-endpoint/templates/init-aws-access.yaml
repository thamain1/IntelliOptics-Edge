apiVersion: v1
kind: ConfigMap
metadata:
  name: azure-bootstrap-notes
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "IntelliOptics-edge-endpoint.labels" . | nindent 4 }}
data:
  README: |
    Azure credentials are provisioned directly by deploy/bin/refresh-ecr-login.sh.
    This ConfigMap exists for backwards compatibility and does not contain active scripts.

