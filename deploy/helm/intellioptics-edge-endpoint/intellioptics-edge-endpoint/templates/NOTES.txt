IntelliOptics Edge Endpoint in the namespace "{{ .Values.namespace }}" has been installed.

Set the `INTELLIOPTICS_ENDPOINT` environment variable to the following URL to use the endpoint in your application:

From your host: 

export INTELLIOPTICS_ENDPOINT="http://localhost:{{ .Values.edgeEndpointPort }}/"

From within the cluster:

export INTELLIOPTICS_ENDPOINT="http://edge-endpoint-service.{{ .Values.namespace }}.svc.cluster.local:30101/"   

